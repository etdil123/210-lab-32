#include "Car-1.h"
#include <deque>

using namespace std;

int main() {
    // constant to store starting # of cars
    int INITIAL = 2;
    srand(time(0));

    // create a deque to store cars
    deque<Car> carDeque;

    

    return 0;
}